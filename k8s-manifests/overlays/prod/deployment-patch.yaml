apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-hub-deployment
spec:
  template:
    spec:
      containers:
      - name: learning-hub-container
        env:
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
